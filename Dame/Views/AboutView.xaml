<Window x:Class="Dame.Views.AboutView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dame.Views"
        mc:Ignorable="d"
        Title="About" Height="350" Width="700">
    <Grid Background="Gainsboro">
        <StackPanel Margin="20,10,0,0">
            <TextBlock Text="{Binding Name, StringFormat='Nume: {0}'}" FontSize="18" Margin="0,0,0,0"/>
            <TextBlock Text="{Binding Email, StringFormat='Email: {0}'}" FontSize="18" Margin="0,10,0,0"/>
            <TextBlock Text="{Binding Group, StringFormat='Grupa: {0}'}" FontSize="18" Margin="0,10,0,0"/>
            <TextBlock Text="{Binding Description}" FontSize="18" TextWrapping="Wrap" Margin="0,10,10,0"/>
        </StackPanel>
        
    </Grid>
</Window>
